interface:
  display_name: "Transcript MD Sync"
  short_description: "Format transcript text and sync Markdown to GitHub."
  default_prompt: "Use $video-subtitle-md-sync to format this transcript into Markdown and push the result to GitHub."
